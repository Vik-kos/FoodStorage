package com.Vikkos.FoodStorage.controller;

//import com.Vikkos.FoodStorage.service.FoodService;

/*
@RestController
@RequestMapping("/api/v1/food")
public class FoodController {

    @Autowired
    private FoodService service;

    @GetMapping
    public ResponseEntity<List<Food>> getFoodList() {
        return new ResponseEntity<List<Food>>(service.findAllFood(), HttpStatus.OK);
    }

    @GetMapping("/{food_id}")
    public ResponseEntity<Optional<Food>> getSingleFoodItem(@PathVariable UUID food_id) {
        return new ResponseEntity<Optional<Food>>(service.findFoodById(food_id), HttpStatus.OK);
    }
}
*/